# Custom aliases
alias ll='ls -lah --color=auto'
alias gs='git status'
alias gp='git pull'
alias gpu='git push'
alias v='nvim'

# Git helpers
alias gpush='git add . && git commit -m "update" && git push'
alias gpull='git pull'

# Projects shortcut
alias proj='cd ~/Projects'

# Ollama shortcuts (if installed)
alias orun='ollama run'
alias olist='ollama list'
alias oshow='ollama show'
